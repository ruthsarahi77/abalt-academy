type Props = { years:number[]; selectedYear:number; onChange:(year:number)=>void; label:string };
export function YearFilter({ years, selectedYear, onChange, label }:Props) { return <div aria-label={label} role="group" className="flex flex-wrap gap-3">{years.map((year)=>{ const selected=year===selectedYear; return <button key={year} type="button" aria-pressed={selected} onClick={()=>onChange(year)} className={`min-h-11 min-w-20 rounded-full border px-5 text-sm font-semibold transition-colors ${selected?"border-abalt bg-abalt text-white":"border-line bg-white text-ink hover:border-abalt hover:text-abalt"}`}>{year}</button>; })}</div>; }
